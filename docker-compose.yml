services:
  backend:
    image: wicara-backend:local
    build:
      context: .
    env_file:
      - .env
    ports:
      - "8000:8000"
    volumes:
      - backend-tmp:/app/tmp
    command: uvicorn app.main:app --host 0.0.0.0 --port 8000
    healthcheck:
      test:
        [
          "CMD",
          "python",
          "-c",
          "import urllib.request; urllib.request.urlopen('http://127.0.0.1:8000/health', timeout=5).read()",
        ]
      interval: 10s
      timeout: 5s
      retries: 12
      start_period: 20s

  media-worker:
    image: wicara-backend:local
    build:
      context: .
    profiles:
      - worker
    env_file:
      - .env
    volumes:
      - backend-tmp:/app/tmp
    command: python -m app.workers.media_worker
    depends_on:
      backend:
        condition: service_healthy

volumes:
  backend-tmp:
